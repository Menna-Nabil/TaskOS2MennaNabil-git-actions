on:
  push:
    branches:
      - master





jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Before Print'
        run: ls

      - uses: actions/checkout@v3

      - name: 'After Print'  
        run: ls

      

     

  